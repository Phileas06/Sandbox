#include <stdio.h>
#define DELTA_A = 1;

int main() {


int umfang, fläche, a;

a DELTA_A

do {
	a = a + 1;
	umfang = 4*a;
	fläche = a*a;
	printf("Seitenlaenge = %d cm \t=> Fläche = %d cmA2 \t und Umfang = %d cm/n",a , fläche, umfang);

} while (umfang != fläche);

return 0;
}
