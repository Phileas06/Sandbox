function [] = addition(matrix1, matrix2) 

    %Überprüfen ob Exakt gleich! (hat lara gesagt)
    if ~(isequal(matrix1, matrix2))
        disp("Matrizen haben nicht die gleiche Größe");
    end

    %Addition elementweise
    maxi = size(matrix1);
    maxi = i(2);
    maxj = size(matrix1);
    maxj = j(1);

    i = 0;
    j = 0;
    sum = [];
    while (i <= maxi)
        while (j <= maxj)
            res = 
            
        end
        sum = [sum, ]
    end

end